global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'iris-classifier'

scrape_configs:
  - job_name: 'iris-api'
    static_configs:
      - targets: ['api:8000']
    metrics_path: '/metrics'
    scrape_interval: 10s
